<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.layout.*?>
<?import javafx.scene.control.*?>
<?import javafx.geometry.Insets?>

<BorderPane fx:id="rootPane"
            fx:controller="com.example.customcourses.controllers.HiddenMainController"
            xmlns:fx="http://javafx.com/fxml"
            prefWidth="1000" prefHeight="600">

    <!-- Barre latérale gauche -->
    <left>
        <VBox>
            <Label fx:id="versionLabel" text="v0.2.0"/>
            <VBox spacing="10" prefWidth="150">
                <padding>
                    <Insets top="10" right="10" bottom="10" left="10"/>
                </padding>
                <Button text="Courses" onAction="#handleHiddenCourses" maxWidth="Infinity"/>
                <Button text="Save Scores" onAction="#handleHiddenScores" maxWidth="Infinity"/>
                <Button text="Musics" onAction="#handleHiddenMusics" maxWidth="Infinity"/>
                <Button text="Course History" onAction="#handleHiddenHistory" maxWidth="Infinity"/>
                <Button text="Back" onAction="#hiddenHandleBack" maxWidth="Infinity"/>
            </VBox>
        </VBox>
    </left>

    <!-- Zone centrale pour le contenu dynamique -->
    <center>
        <StackPane fx:id="hiddenMainContent">
            <padding>
                <Insets top="10" right="10" bottom="10" left="10"/>
            </padding>
        </StackPane>
    </center>
</BorderPane>
